/* =========================================================
   CANE RUSH — main.js
   Boot. Everything above this file has finished defining CaneRush
   and attaching its methods by the time DOMContentLoaded fires.
   ========================================================= */
'use strict';

window.addEventListener('DOMContentLoaded', ()=>{
  window.__game = new CaneRush();
});
