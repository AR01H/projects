/**
 * assets/js/pages/testing.js - page-specific JS for /testing/ (Component
 * Showcase). Empty on purpose: every interaction demoed on this page
 * (tabs, dialogs, the wizard modal, ajax forms, lightbox) is already wired
 * globally by assets/js/common.js and assets/js/ui-kit.js. Kept as a real
 * file - not just a config/pages.php reference - so it is here the moment a
 * showcase-only interaction actually needs one.
 */
